{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capability boundary for the shared Memi Canvas client. Native access is limited to a user-initiated repository folder picker.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
